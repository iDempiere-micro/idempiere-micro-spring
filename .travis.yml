sudo: required

language: java
install: true

jdk:
  - oraclejdk8

services:
  - docker

before_install:
  - ./before_install.sh

script:
  # on Travis we need to run the docker image on port 5433
  - sed -i 's/5432/5433/g' etc/idempiere.properties
  - make package
